language: ruby

before_install:
  # - sudo apt-get -qq update
  # - sudo apt-get install -qq scriptroute
  # - sudo apt-get install -qq pathchar
  - sudo apt-get install libpcap-dev
  - sudo apt-get install traceroute
  - curl -O http://www.kitchenlab.org/www/bmah/Software/pchar/pchar-1.5.tar.gz

install:
  - tar zxf pchar-1.5.tar.gz
  - ( cd pchar-1.5 && ./configure --with-ipv6 --prefix=../ && make install )

before_script:
  - sbin/pchar --help

script: ./script.sh